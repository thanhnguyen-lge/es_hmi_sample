include: ./analysis_options_package_3.22.2.yaml

analyzer:
  language:
    strict-inference: true
  errors:
    invalid_assignment: error
    missing_return: error
    dead_code: error
    todo: error
    # allow deprecated members (we do this because otherwise we have to annotate
    # every member in every test, assert, etc, when we or the Dart SDK deprecates
    # something (https://github.com/flutter/flutter/issues/143312)
    deprecated_member_use: ignore
  exclude: # DIFFERENT FROM FLUTTER/FLUTTER
    # Ignore generated files
    - '**/*.g.dart'
    - '**/*.gr.dart'
    - '**/*.freezed.dart'
    - '**/generated_*.dart'

# linter:
#   rules:
#     # analysis_options_flutter_3.22.2.yaml
#     - avoid_bool_literals_in_conditional_expressions: false
#     - avoid_dynamic_calls: false
#     - avoid_equals_and_hash_code_on_mutable_classes: false
#     - avoid_setters_without_getters: false
#     - no_default_cases: false
#     - prefer_asserts_in_initializer_lists: false
#     - prefer_foreach: false
#     - use_setters_to_change_properties: false
#     # analysis_options_package_3.22.2.yaml
#     - public_member_api_docs: false
